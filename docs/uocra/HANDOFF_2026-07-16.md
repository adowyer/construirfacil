# Handoff Sindicato RDLS — 2026-07-16

> Día de la **tanda 2** (8 fichas por chat → 7 mails), del **workspace de las asesoras en HubSpot**,
> del **tracking de clics** (vivo) y de la saga del **mail invisible** (rodeada por código).
> Entrada anterior: `HANDOFF_2026-07-14.md`. ⚠️ Renombre público: **"UOCRA" → "Sindicato RDLS"**
> (campaña renombrada en Supabase + HubSpot; las llaves internas `source='sindicato_uocra'`,
> carpetas y scripts NO se tocaron — nadie las ve y renombrarlas rompe queries).

---

## 1. Tanda 2 — 8 fichas nuevas (ENVIADA)

Fichas recibidas **por chat como fotos** (no por Drive). Flujo cuidado, prototipo del ingest sano:
extracción por Claude → **revisión de Andrea contra el papel** (4 correcciones: mail de Rodriguez
Quiroz, sueldo de Saso $2M, DNI de Vega 40616044 —el CUIL validó su corrección—, fecha de Sepulveda)
→ dup-check contra la base (por DNI+tel+mail+nombre, no solo DNI) → insert con llave por ficha
(`source_file='FichasChat-2026-07-16'`+page) → calificación con el motor → **7 mails (0 fallidos)**.

- **Jara Evelin Giselle sin mail** (escribió el CUIL en el campo Mail) → WhatsApp manual (+los 6 de tanda 1 = 7 pendientes).
- **Nievas**: sueldo escrito como RANGO 1,6–9M → se tomó el **PISO** ($1.6M, USD 62.837). Confirmar en llamada (solo puede mejorar).
- **Saso y Sepulveda**: marcaron "primera vivienda: NO" → línea Banco Nación con ~mitad de crédito. Si entendieron mal la pregunta, corregir en llamada les **duplica** el crédito.
- Total acumulado: **40 mails enviados** (33 + 7). Gotchas de PostgREST aprendidos: bulk insert exige
  claves uniformes en todas las filas; `contract_type` check en minúscula (`'efectivo'/'temporario'`).
- Al sincronizar a HubSpot: los BCC habían auto-creado 7 contactos pelados → **el sync por `lead_id`
  los detectó (409) y los enriqueció en vez de duplicar**. Primera corrida real del fix: 0 duplicados.

## 2. Ingest OCR — el fix quedó COMPLETO (estaba a medias)

La sesión "rara" del 15-jul había escrito el fix (llave por ficha) pero **sin commitear y sin pushear
a n8n** (el workflow vivo seguía roto — mi primer chequeo dio falso positivo por un UPDATE de backfill
que contenía 'source_file'; el `updatedAt` lo delató). Además el fix tenía una **mina**: las filas
viejas tienen `source_file` pero NO `page` → re-correr un listado viejo habría duplicado ~294 leads.
- Rescatado y commiteado (`d4f803b`), **guarda legacy** agregada (`804d46f`), **pusheado a n8n y
  verificado en el WHERE vivo**. `INCLUDE_NAME_SUBSTR` para procesar solo PDFs puntuales.
- Falla nueva de OCR detectada (caso Caitruz): **checkbox con DOBLE MARCA la lee como "Sí" con
  total confianza** → agregar al extractor: doble marca ⇒ flag, nunca leer confiado.

## 3. HubSpot — el workspace de las asesoras (Alejandra · Romina · Zoe)

- **Vista "📞 Mails enviados"**: filtrada por `Mail enviado` is known → crece sola con cada tanda.
- **Propiedades nuevas**: `engagement_sent_at` («Mail enviado», sync automático), `email_verified_at`
  («✉️ Registro verificado», sync automático), `assigned_advisor` («Asesora asignada»:
  Alejandra/Romina/Zoe/Sin asignar — la reparten ellas; tip: selección múltiple en la vista → Editar),
  y grupo **«🤖 Ximia · Discovery»**: `family_size`, `children_count`, `interest_level`
  (alto/medio/bajo/sin contacto), `special_needs` + detalle. Los llena la asesora en la llamada;
  a futuro los infiere Ximia. `residency_years` ya existía (= "años en Neuquén").
- **`has_lot` ganó la opción «🔍 A confirmar»** (caso Caitruz: ficha con doble marca; la gestora lo
  había bajado a mano el 10-jul — su bucket se respeta; nota en Supabase para la llamada).
- **Notas por contacto (40/40)**: 📌 fijada «ORIGEN DEL CONTACTO» (papel → verificado → precalificado
  con el motor de Ximia → mail → llamar) + «✉️ MAIL ENVIADO» con el contenido personalizado
  (crédito/cuota/línea/casas, regenerado con la MISMA función del mailer).
- **Créditos HubSpot = mail**: 5 fichas estaban viejas (Jerónimo 207k→99k, Carrizo, Carrasco,
  Margarita, Virginia) → refrescadas; verificación campo×campo 40/40 alineados.
  ⚠️ Systemic: el sync es one-shot → falta modo `--refresh-derived` (backlog).
- **Breeze AI**: su resumen confundía ("considerá enviar un correo") → tarjeta sacada de la vista
  del equipo; la nota fijada manda.
- **Ficha de llamada**: tarjeta "Lista de propiedades" con los campos Discovery (Andrea la arma en
  Personalizar, ya lo hizo antes). Llamadas → botón "Llamada" (datos duros en tarjeta, relato en actividad).

## 4. 🔍 El mail invisible (misterio abierto en HubSpot, RODEADO por código)

El usuario compartido "Gestión" **no ve los objetos email** logueados por BCC, tras agotar TODO:
re-login, filtros (23/32 → todo tildado), sub-pestaña Correos (aparece pero vacía), membresía de la
bandeja `hola@`, seat, y el permiso "acceso a correos del CRM = todos" (ese cambio hizo APARECER la
sub-pestaña, pero sigue sin contenido). Los 40 emails EXISTEN asociados (verificado por API).
- **Workaround definitivo (regla de oro)**: el contenido del mail vive como NOTA (§3) — visible
  comprobado. El mailer ahora la crea **automáticamente en cada envío** (`3862ca3`).
- Pendiente sin urgencia: ticket a soporte de HubSpot.

## 5. Tracking de clics — VIVO ✅

- Migración **0096** (`lead_link_clicks`) corrida · ruta `/api/track/click` deployada (`cf9df87`)
  · **probada en prod**: firma válida redirige+loguea; firma trucha redirige SIN loguear (el link
  jamás se rompe); open-redirect bloqueado. Filas de prueba borradas.
- **`CLICK_TRACKING_ENABLED=1` ACTIVADA** en `.env.local` → la próxima tanda sale con links
  trackeados (sin la flag, links directos — un mail jamás sale roto por orden de deploy).
- `reporte_engagement.py` muestra «🏠 miró: casa-x» por lead. ⚠️ Los 40 ya enviados llevan links
  directos — el tracking aplica de la tanda 3 / follow-ups en adelante.

## 6. Operación de llamados

- **Reporte diario**: `python3 scripts/reporte_engagement.py` (panel: verificados → llamar primero,
  orden por crédito, 💰=anticipo, WhatsApp pendientes, clics).
- Verificados al cierre: **1/40** (Anahi Macarena Lara, 15-jul — llamarla primero). Timing malo para
  medir: martes noche + miércoles de semifinal. "No verificó ≠ no lo vio" (sin tracking de apertura).
- **WhatsApp de no-atendidos** (4-5/día desde el tel de cada asesora, variantes A/B redactadas en la
  sesión): nombra el sindicato (ancla de confianza), NO dice el monto (pantalla bloqueada), cierra
  pidiendo horario. Registrar el touch en la ficha.
- Rebotes: dashboard de Resend (la API key es solo-envío).

## 7. Backlog

| # | Qué | Nota |
|---|---|---|
| 1 | WhatsApp manual a los 7 sin mail | Andrea/asesoras |
| 2 | **Reprocesar los 274** (150 discrepancias HS↔SB) | bajar → recalificar → subir; NUNCA sync directo (pisa correcciones) |
| 3 | Modo `--refresh-derived` en reconcile | re-empuja crédito/fechas al recalcular; nunca identidad ni campos de gestoras |
| 4 | Renombres internos "UOCRA" | workflow n8n «UOCRA OCR Ingest» (+builder, matchea por nombre), header del reporte |
| 5 | Ticket soporte HubSpot | email invisible para usuarios no-admin (§4) |
| 6 | Chip + WhatsApp API (Meta Cloud vía n8n) | chip solo recibe el código UNA vez; NO virtual/VoIP |
| 7 | Extractor: flag para checkbox con doble marca | §2 |
| 8 | Lead score fresco para los 321 (fórmula de junio) | opcional, Andrea decide |
